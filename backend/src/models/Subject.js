class Subject {
    constructor(id, name, userId, createdAt) {
        this.id = id;
        this.name = name;
        this.userId = userId;
        this.createdAt = createdAt;
    }
}

module.exports = Subject;
